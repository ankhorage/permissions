---
'@ankhorage/permissions': minor
---

Add an optional Expo permission client entrypoint and manifest metadata, and tighten the Expo adapter tooling so strict lint, typecheck, and package verification pass without requiring Expo packages in the core workspace.
